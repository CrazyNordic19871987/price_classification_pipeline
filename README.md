# Car Price Classification Pipeline

Проект по машинному обучению для многоклассовой классификации цен автомобилей. Реализован полный sklearn-пайплайн предобработки данных и обучения моделей.

## 🎯 Задача

Многоклассовая классификация для предсказания категории цены автомобиля (high, medium, low) на основе его характеристик.

## 📊 Данные

Используется датасет с информацией об автомобилях, содержащий 19 признаков и 10,000 записей.

**Целевая переменная:** `price_category` (high/medium/low)

## 🛠 Технологии

- Python 3.8+
- pandas
- numpy
- scikit-learn
- pickle

## 🚀 Особенности проекта

### Пайплайн предобработки данных:

1. **Фильтрация датасета** - удаление ненужных колонок
2. **Обработка выбросов** - сглаживание аномалий в колонке `year`
3. **Feature Engineering** - создание новых признаков:
   - `short_model` - первое слово из названия модели
   - `age_category` - категоризация возраста автомобиля
4. **Обработка числовых признаков** - заполнение пропусков медианой + стандартизация
5. **Обработка категориальных признаков** - заполнение пропусков модой + one-hot encoding

### Модели машинного обучения:

- Logistic Regression
- Random Forest Classifier  
- Support Vector Classifier (SVC)

## 📁 Структура проекта


car-price-classification/
├── main.py # Основной скрипт с пайплайном
├── car_price_pipeline.pkl # Сохраненная модель (создается после запуска)
├── data/
│ └── homework.csv # Исходные данные
└── README.md



## 🏃‍♂️ Запуск проекта

1. Клонируйте репозиторий:
```bash
git clone https://github.com/yourusername/car-price-classification.git
cd car-price-classification

📊 Результаты
После выполнения скрипт:

Проводит кросс-валидацию трех моделей

Выбирает лучшую модель по метрике accuracy

Сохраняет полный пайплайн в файл car_price_pipeline.pkl

Выводит результаты предсказаний

Пример вывода:

Car Price Classification Pipeline
==================================================
Загрузка данных...
Данные успешно загружены. Размер: (10000, 19)

Оценка моделей на кросс-валидации:
--------------------------------------------------
model: LogisticRegression        acc_mean: 0.7751, acc_std: 0.0042
model: RandomForestClassifier    acc_mean: 0.7699, acc_std: 0.0023
model: SVC                       acc_mean: 0.7612, acc_std: 0.0031
--------------------------------------------------
best model: LogisticRegression, accuracy: 0.7751

FunctionTransformer для кастомных преобразований:
python
preprocessing_pipeline = Pipeline(steps=[
    ('filter', FunctionTransformer(filter_data)),
    ('outlier_handling', FunctionTransformer(handle_outliers)),
    ('feature_engineering', FunctionTransformer(create_features))
])
Автоматический выбор признаков:
python
feature_processor = ColumnTransformer(
    transformers=[
        ('num', numerical_transformer, make_column_selector(dtype_include=['int64', 'float64'])),
        ('cat', categorical_transformer, make_column_selector(dtype_include=object))
    ]
)
📈 Возможности улучшения
Добавление feature selection

Оптимизация гиперпараметров

Эксперименты с другими алгоритмами

Добавление метрик кроме accuracy

Создание API для предсказаний


